server:
  port: 1901  # 服务器访问端口

spring:
  datasource:
    url: jdbc:mysql://47.107.52.39:3306/demo_springCloud
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  main:
    allow-bean-definition-overriding: true
  jpa:
    database: MYSQL
    show-sql: true                 # 是否显示后台处理的SQL语句
    hibernate.ddl-auto: update     # 自动检查实体和数据库表是否一致，如果不一致则会进行更新数据库表

# 调整日志级别
logging:
  level:
    root: warn